[pytest]
# Put the repo root on sys.path so `server` imports resolve regardless of how
# pytest is launched (`pytest`, `uv run pytest`, or `python -m pytest`).
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
